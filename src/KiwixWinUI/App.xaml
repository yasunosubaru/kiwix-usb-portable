<?xml version="1.0" encoding="utf-8"?>
<Application
    x:Class="KiwixWinUI.App"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <XamlControlsResources xmlns="using:Microsoft.UI.Xaml.Controls" />
            </ResourceDictionary.MergedDictionaries>

            <!--
                WinUI 3 is NOT WPF. Only properties that actually exist on the
                target type may be set here; an unknown one makes the XAML
                compiler abort with no diagnostic at all.
                Specifically: Button has no CornerRadius, and TextBox has no
                IsScrollBarEnabled. Both exist in WPF and are easy to carry over
                by mistake.
            -->

            <Style x:Key="CardBorder" TargetType="Border">
                <Setter Property="Background" Value="#1B1F2A" />
                <Setter Property="CornerRadius" Value="10" />
                <Setter Property="Padding" Value="18,14,18,14" />
                <Setter Property="BorderBrush" Value="#262B3A" />
                <Setter Property="BorderThickness" Value="1" />
            </Style>

            <Style x:Key="CardTitle" TargetType="TextBlock">
                <Setter Property="Foreground" Value="#8B93A7" />
                <Setter Property="FontSize" Value="12" />
                <Setter Property="Margin" Value="0,0,0,10" />
            </Style>

            <Style x:Key="Body" TargetType="TextBlock">
                <Setter Property="Foreground" Value="#E6E9F0" />
                <Setter Property="FontSize" Value="14" />
            </Style>

            <Style x:Key="Dim" TargetType="TextBlock">
                <Setter Property="Foreground" Value="#8B93A7" />
                <Setter Property="FontSize" Value="12" />
            </Style>

            <Style x:Key="Mono" TargetType="TextBox">
                <Setter Property="Foreground" Value="#C8D0E0" />
                <Setter Property="Background" Value="#0E1117" />
                <Setter Property="BorderThickness" Value="0" />
                <Setter Property="IsReadOnly" Value="True" />
                <Setter Property="TextWrapping" Value="Wrap" />
                <Setter Property="AcceptsReturn" Value="True" />
                <Setter Property="FontFamily" Value="Cascadia Mono, Consolas" />
                <Setter Property="FontSize" Value="12" />
            </Style>

            <Style x:Key="PrimaryButton" TargetType="Button" BasedOn="{StaticResource AccentButtonStyle}">
                <Setter Property="Background" Value="#4C8DFF" />
                <Setter Property="Foreground" Value="#0B1020" />
                <Setter Property="FontWeight" Value="SemiBold" />
                <Setter Property="Padding" Value="20,9,20,9" />
            </Style>
        </ResourceDictionary>
    </Application.Resources>
</Application>
